exclude: "docs/_build|docs/source/auto_examples|docs/source/reference/generated|node_modules|migrations|.git|.tox"
default_stages: [pre-commit]
fail_fast: true

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-toml
      - id: check-yaml
        exclude: conda/meta.yaml

  # Can run individually with `pre-commit run black --all-files`
  - repo: https://github.com/psf/black
    rev: 26.5.1
    hooks:
      - id: black

  # Can run individually with `pre-commit run isort --all-files`
  - repo: https://github.com/timothycrosley/isort
    rev: 9.0.0b2
    hooks:
      - id: isort
        args:
          - --profile
          - black


  # Numpydoc
  - repo: https://github.com/numpy/numpydoc
    rev: v1.11.0rc0
    hooks:
      - id: numpydoc-validation
        # sphinx-gallery examples open with an RST title, not a numpydoc
        # summary — the title is what the gallery hangs its label on.
        # benchmarks/ holds stand-alone measurement scripts, not API.
        exclude: ^(docs/source/conf\.py|examples/.*\.py|benchmarks/.*\.py)$

  # Can run individually with `pre-commit run mypy --all-files`
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v2.3.0
    hooks:
      - id: mypy
        args: ["--config-file", "pyproject.toml", "--ignore-missing-imports"]
        additional_dependencies: [types-toml, types-requests]


  - repo: https://github.com/srstevenson/nb-clean
    rev: 4.0.1
    hooks:
     - id: nb-clean

  - repo: local
    hooks:
      - id: pylint
        name: pylint
        entry: pylint
        language: system
        types: [python]
        exclude: ^benchmarks/
        require_serial: true
        args:
          [
            "-rn", # Only display messages
            "-sn", # Don't display the score
            "--extension-pkg-whitelist=scipy,netCDF4",
          ]


  - repo: local
    hooks:
    - id: pytest
      name: pytest
      entry: pytest tests
      language: system
      types: [python]
      exclude: ^venv/ ^.git/ ^.vscode/ ^.DS_Store ^uq/ ^hindcasts/
      always_run: true
      pass_filenames: false
      stages: [pre-push]
