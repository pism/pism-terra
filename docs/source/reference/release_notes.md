# Release notes

pism-terra uses [Semantic Versioning](https://semver.org). Versions are
derived from git tags via `setuptools_scm`.

## Unreleased

- Initial Sphinx documentation skeleton (this site).

```{admonition} TODO
- Adopt a CHANGELOG.md at the repo root and include it here via `{include}`.
- Cut a first tagged release.
```
