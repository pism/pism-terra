# Geometry notebook

```{admonition} TODO
- Adapted from `notebooks/geometry.ipynb`.
- Drop the notebook into `docs/source/examples/geometry.ipynb` and update
  the `toctree` in {doc}`./index` to point at the `.ipynb`.
```
